import math
square_side = float(input('What is the length of one side of the square? '))

print(f'the area of the Square is {square_side * square_side } ')

rec_leng = float(input('What is the lengnth of the rectangle? '))
rec_width = float(input('What is the width of the rectangle? '))

print(f'the area of the rectangle is {rec_leng * rec_width}')



circle_rad = float(input( 'what is the radius of the circle? '))

print(f'the area of the circle is {circle_rad * math.pi')
